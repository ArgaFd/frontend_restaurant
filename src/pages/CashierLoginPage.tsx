import LoginPage from './LoginPage';

// Login khusus untuk Cashier, menggunakan form login staff yang sama
const CashierLoginPage = () => {
  return <LoginPage />;
};

export default CashierLoginPage;
